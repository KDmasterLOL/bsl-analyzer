[package]
name = "project-model"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
chrono.workspace = true
url.workspace = true
toml = "1"
blake3 = "1.8"
base-db.workspace = true
bsl-metadata = { path = "../bsl-metadata" }
stdx.workspace = true
walkdir.workspace = true

[dev-dependencies]
expect-test.workspace = true
tempfile.workspace = true

# A symlink cycle the kernel rejects (a file linking to itself, two links pointing at
# each other) arrives as a bare ELOOP, and `io::ErrorKind::FilesystemLoop` is unstable,
# so the errno is the only stable way to tell that class from an unreadable file.
[target.'cfg(unix)'.dependencies]
libc.workspace = true

# Test-only: creating a FIFO is the one way to reproduce a blocking open, and
# the guard against it has no other observable failure than a hang.
[target.'cfg(unix)'.dev-dependencies]
libc.workspace = true
