# Пропущен постфикс "Клиент" (CommonModuleNameClient)

## Описание диагностики

Клиентские общие модули предназначены для размещения бизнес-логики, выполняемой исключительно на стороне клиента. У таких модулей установлены признаки `Клиент (управляемое приложение)` и/или `Клиент (обычное приложение)`.

Имя клиентского общего модуля должно заканчиваться постфиксом `Клиент` (англ. `Client`). Исключение составляют модули с установленным флагом `Глобальный` -- для них постфикс не требуется.

В отдельных случаях, когда клиентский код предназначен только для управляемого приложения, только для обычного приложения или только для внешнего соединения, допускается иная комбинация клиентских признаков.

## Примеры

Допустимые имена: `РаботаСФайламиКлиент`, `ОбщегоНазначенияКлиент`, `StandardSubsystemsClient`

Недопустимые имена: `РаботаСФайлами`, `ОбщегоНазначения` (при клиентских признаках модуля)

## Источники

* [Стандарт: Тексты модулей](https://its.1c.ru/db/v8std#content:469:hdoc:2.3)
