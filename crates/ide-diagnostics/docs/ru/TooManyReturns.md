# Метод не должен содержать много возвратов (TooManyReturns)

## Описание диагностики

Чрезмерное количество операторов `Возврат` в процедуре или функции усложняет понимание логики работы метода и затрудняет его отладку. Рекомендуется структурировать код так, чтобы метод имел минимальное число точек выхода.

## Примеры

Плохо:

```bsl
Функция ПолучитьСкидку(Категория)
    Если Категория = "VIP" Тогда
        Возврат 30;
    ИначеЕсли Категория = "Постоянный" Тогда
        Возврат 15;
    ИначеЕсли Категория = "Новый" Тогда
        Возврат 5;
    ИначеЕсли Категория = "Сотрудник" Тогда
        Возврат 20;
    КонецЕсли;
    Возврат 0;
КонецФункции
```

Лучше:

```bsl
Функция ПолучитьСкидку(Категория)
    Скидка = 0;
    Если Категория = "VIP" Тогда
        Скидка = 30;
    ИначеЕсли Категория = "Постоянный" Тогда
        Скидка = 15;
    ИначеЕсли Категория = "Новый" Тогда
        Скидка = 5;
    ИначеЕсли Категория = "Сотрудник" Тогда
        Скидка = 20;
    КонецЕсли;
    Возврат Скидка;
КонецФункции
```

## Источники

* [Why Many Return Statements Are a Bad Idea in OOP](https://www.yegor256.com/2015/08/18/multiple-return-statements-in-oop.html)
* [JAVA: Methods should not have too many return statements](https://rules.sonarsource.com/java/RSPEC-1142)
* [Почему ранний возврат из функций так важен?](https://habr.com/ru/post/348074/)
