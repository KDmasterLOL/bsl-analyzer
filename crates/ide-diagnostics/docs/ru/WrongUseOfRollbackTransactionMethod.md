# Некорректное использование метода ОтменитьТранзакцию() (WrongUseOfRollbackTransactionMethod)

## Описание диагностики

Метод `ОтменитьТранзакцию` должен вызываться внутри блока `Исключение` конструкции `Попытка-Исключение`. При этом `ОтменитьТранзакцию` должен быть первым значимым действием в блоке обработки ошибки -- до записи в журнал регистрации и других операций.

Текущая реализация срабатывает, если вызов:

- расположен вне конструкции `Попытка ... Исключение`;
- находится в основном блоке `Попытка`;
- расположен в ветке `Исключение`, но перед ним есть исполняемый код.

## Примеры

### Неправильно

```bsl
НачатьТранзакцию();
Попытка
    Документ.Записать();
    ЗафиксироватьТранзакцию();
Исключение
    ЗаписьЖурналаРегистрации("Ошибка записи",
        УровеньЖурналаРегистрации.Ошибка,,,
        ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
    ОтменитьТранзакцию(); // Неверно: откат должен быть первым действием
    ВызватьИсключение;
КонецПопытки;
```

### Правильно

```bsl
НачатьТранзакцию();
Попытка
    Документ.Записать();
    ЗафиксироватьТранзакцию();
Исключение
    ОтменитьТранзакцию();
    ЗаписьЖурналаРегистрации("Ошибка записи",
        УровеньЖурналаРегистрации.Ошибка,,,
        ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
    ВызватьИсключение;
КонецПопытки;
```

## Источники

- [Стандарт: Транзакции: правила использования](https://its.1c.ru/db/v8std#content:783:hdoc:_top)
- [v8std.ru: WrongUseOfRollbackTransactionMethod](https://v8std.ru/diagnostics/bslls/WrongUseOfRollbackTransactionMethod/)
