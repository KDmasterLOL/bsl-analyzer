[package]
name = "module-graph"
version = "0.1.0"
edition = "2021"
rust-version = "1.80"
license = "MIT OR Apache-2.0"
authors = ["BSL Analyzer Contributors"]

[lib]

[dependencies]
# Workspace dependencies
base-db = { path = "../base-db" }
vfs = { path = "../vfs" }
syntax = { path = "../syntax" }
ide-db = { path = "../ide-db" }
bsl-metadata = { path = "../bsl-metadata" }

# External dependencies
la-arena = "0.3"
rustc-hash = "2.0"
tracing = { workspace = true }

[dev-dependencies]
parser = { path = "../parser" }
salsa.workspace = true
