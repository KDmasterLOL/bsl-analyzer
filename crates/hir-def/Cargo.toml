[package]
name = "hir-def"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
cfg-types.workspace = true
base-db.workspace = true
syntax.workspace = true
vfs.workspace = true
intern.workspace = true
salsa.workspace = true
rustc-hash.workspace = true
la-arena.workspace = true
ordered-float.workspace = true
smol_str.workspace = true
smallvec.workspace = true
text-size.workspace = true
tracing.workspace = true
# NOTE: cfg dependency creates circular crate dependency (hir-def → cfg → hir-def)
# However, this is acceptable because:
# 1. Cargo supports circular crate dependencies (not same as type cycles)
# 2. No circular type dependencies: cfg uses hir-def types (StmtId, ExprId), hir-def uses CFG for analysis
# 3. Enables CFG-based diagnostics in hir-def
# TODO: Uncomment after cfg-types refactoring completes
# cfg.workspace = true
parser.workspace = true
bsl-metadata.workspace = true
bsl-platform.workspace = true
line-index.workspace = true

[dev-dependencies]
expect-test.workspace = true
lexer.workspace = true
parser.workspace = true
ide-db.workspace = true
