// Минимальное тело модуля, объявленного только в CFE.
// Достаточно для loader'а bsl-metadata, который требует наличия
// `CommonModules/<Name>/` как директории и опционально использует
// `Ext/Module.bsl` как маркер источника.

Процедура ПриветИзРасширения() Экспорт
КонецПроцедуры
