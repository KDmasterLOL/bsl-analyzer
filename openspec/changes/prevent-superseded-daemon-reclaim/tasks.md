## 1. Необратимое состояние аренды

- [x] 1.1 Добавить общий `AtomicBool superseded` без изменения `LeaseRecord`; сериализовать process-local наблюдение чужого токена, фиксацию terminal flag и решение о повторном захвате одним mutex состояния аренды, переиспользовав защищающий `checked_at` lifecycle state. Устанавливать признак в `recheck`, когда ранее успешно захвативший аренду экземпляр читает корректную неустаревшую запись с чужим токеном, и не сбрасывать при исчезновении, устаревании либо `release` чужой записи. Проверка: `cargo test -p mcp-server workspace_lease::tests::observed_foreign_owner_is_permanent` покрывает прежний формат записи, освобождение владельца и успешный захват третьим процессом.
- [x] 1.2 Фиксировать `superseded` при первом корректном неустаревшем чужом токене в `with_ownership`; во всех путях соблюдать порядок `lease lifecycle mutex → lease file lock`, а замыкание или запись аренды выполнять только пока lifecycle guard подтверждает отсутствие terminal flag. Устаревший чужой токен запрещает текущее замыкание, но не устанавливает терминальный признак и допускает последующий штатный повторный захват. Проверка: `cargo test -p mcp-server workspace_lease::tests::publish_fence_latches_supersession` и детерминированный `cargo test -p mcp-server workspace_lease::tests::observed_owner_race_cannot_reclaim`, который ставит reclaim между чтением чужого токена и фиксацией flag и доказывает отсутствие записи.
- [x] 1.3 Добавить `WorkspaceLease::is_superseded()` и `is_released()` как чтение соответствующих process-local признаков, не равное `!owns_caches()`, и сохранить повторный захват для никогда не владевшего `UNCLAIMED`; после фиксации supersession `owns_caches`, `owns_caches_now`, `with_ownership` и `take_generation` должны отказывать до disk I/O или попытки захвата. Проверка: `cargo test -p mcp-server workspace_lease::tests::transient_unclaimed_is_not_superseded` также различает release и supersession и считает обращения к test seam аренды после терминальной фиксации.
- [x] 1.4 Сохранить действующие границы восстановления: удаление `.build`, корректная устаревшая, повреждённая и незамеченная краткая чужая запись не создают вытеснение, а неуправляемая аренда остаётся fail-open. Инвертировать `releasing_on_exit_returns_the_workspace_to_the_older_daemon`, переименовать/переформулировать `an_abandoned_lease_is_reclaimed_by_the_superseded_daemon` как контроль невиденной уже устаревшей записи и обновить комментарии `workspace_lease.rs` о возврате владения; проверка: `cargo test -p mcp-server workspace_lease::tests::non_witness_states_remain_reclaimable` и остальные тесты модуля `workspace_lease`.

## 2. Запрет защищённых записей

- [x] 2.1 Поставить терминальную проверку вытеснения графа до `try_publish_cached`, stale-adoption и reload; инвертировать `a_graph_that_stood_aside_builds_once_the_workspace_comes_back` и обновить комментарии `graph/state.rs` о возврате владения. Проверка: `cargo test -p mcp-server graph::state::tests::superseded_graph_never_adopts_or_reloads` доказывает отсутствие adoption, loader и reload после освобождения нового владельца.
- [x] 2.2 Добавить в `bsl-search` fenced-варианты трёх существующих workspace-конструкторов с результатом `Result<Option<SearchEngine>, SearchError>`, где `None` означает callback refusal, а `Err` — реальную ошибку; прежние public wrappers делегируют им без fence и сохраняют сигнатуры/source/runtime-поведение. В fenced-вариантах открытие/миграции Store и FTS rebuild выполнять отдельными apply-группами callback, чтение либо построение HNSW — вне callback, а best-effort sidecar persist — ещё одной apply-группой; использовать `Store::open_existing` там, где файл уже обязан существовать. Проверка: `cargo test -p bsl-search engine::tests::workspace_constructor_publish_fence` покрывает каждый режим и outcome, отказ до migration/FTS/sidecar и доказывает, что долгий index build не удерживает callback.
- [x] 2.3.1 Передать `WorkspaceLease` в `init_workspace_search_engine`, добавить один локальный startup-callback: временный `with_ownership` refusal не вызывает apply, ждёт существующий `VERDICT_TTL` и повторяет захват; `is_superseded()` либо `is_released()` возвращает terminal no-publish. Перевести init на `Result<Option<WorkspaceSearchInit>, SearchError>` и вызвать fenced-конструкторы задачи 2.2, не повторяя HNSW build. Проверка: `cargo test -p mcp-server state::bootstrap::tests::startup_constructor_fence_distinguishes_retry_terminal_and_error`.
- [x] 2.3.2 Проводить настройку roots/mode и все clear/save baseline manifest/overlay metadata через тот же callback; разрешение snapshot и внешнюю загрузку manifest оставлять вне lease lock, а подготовленный результат применять ровно один раз. Реальные project/baseline/Store ошибки возвращать как `Err`, terminal refusal — как `Ok(None)`. Проверка: `cargo test -p mcp-server state::bootstrap::tests::startup_metadata_fence_stops_after_takeover`.
- [x] 2.3.3 Разделить standalone ingest/reconcile на подготовку вне lease lock и минимальные apply-группы через startup-callback; при временном отказе повторять только apply, не scan/read/parse, а при terminal отказе сохранять уже ограждённые файлы и прекращать bootstrap. Проверка: `cargo test -p mcp-server state::bootstrap::tests::startup_ingest_fence_does_not_repeat_preparation`.
- [x] 2.3.4 Оградить post-init `initialize_workspace_overlay_clean`/`prime_workspace_overlay` и последнюю публикацию engine/status тем же callback; после terminal `None` не запускать graph, sink или embed, не передавать cursor и overflow, а superseded-status выставлять только по `is_superseded()`. Проверка: `cargo test -p mcp-server state::bootstrap::tests::superseded_bootstrap_stops_mutating` покрывает takeover перед каждой apply-группой, после возврата init и перед публикацией; release во время retry быстро завершает worker без apply и публикации.
- [x] 2.4 Передать `WorkspaceLease` в `FusedChunkWriter`: группировку, чтение и hash-check выполнять вне блокировки, а каждый `ingest_fused_file` для одного файла коммитить внутри `with_ownership`; отказ прекращает fused ingest, сохраняет уже успешно ограждённые файлы и удаляет только temp-граф текущей сборки до fallback. Проверка: `cargo test -p mcp-server graph::build::tests::superseded_fused_writer_stops_mutating` моделирует takeover перед двумя последовательными per-file commit и проверяет cleanup текущего `*.building.*`.
- [x] 2.5.1 Добавить единый host barrier с порядком `engine mutex → lease lifecycle mutex → lease file lock` и разделить hub drain на materialize/ack: cursor/rescan debt меняются только после успешного apply, новый drift между фазами остаётся следующим batch. Старый `drain` сохранить wrapper-ом. Проверка: точный unit-тест change hub на отказ, повтор того же batch и ack.
- [x] 2.5.2 Перевести `spawn_search_sink` и все drift-family мутации (`enable_workspace_watcher_mode`, dirty/context marking, file/subtree deletion, overflow reconcile/whole-context mark) на host barrier; при transient сохранять materialized batch и повторять, при terminal прекращать sink без ack/записи. Проверка: первая часть table-driven `state::sync::tests::superseded_daemon_cannot_mutate_shared_search` доказывает cursor semantics и неизменность Store.
- [x] 2.5.3 Провести через тот же barrier resident-fed `reindex_dirty_from_snapshots` и root/context/embedding apply, оставив подготовку вне lock; reference-профиль и прямые unmanaged `bsl-search` вызовы не менять. Расширить `state::sync::tests::superseded_daemon_cannot_mutate_shared_search` на оставшиеся apply-family и доказать повтор обязательств после transient refusal.
- [x] 2.6 Перед каждым из `notify_published`, `flush_pending_topology_refresh`, `flush_pending_search_roots_refresh`, `consume_leftover_marks` выполнять свежую `owns_caches_now()` до очистки пула, запуска reload или снятия pending/leftover; provider и поисковые применения проводить через барьер задачи 2.5 и не устанавливать provider в память при отказе. Проверка: `cargo test -p mcp-server graph::state::tests::superseded_late_publish_is_fenced` моделирует takeover после rename/adoption и между plan/apply.
- [x] 2.7 В `bsl-search` переиспользовать существующие overlay plan/publish и добавить явный read-only режим `workspace_overlay_snapshot`, который возвращает текущий resident snapshot без fingerprint, manifest или Store-мутаций. Протянуть fenced apply и этот fallback через lexical, semantic и hybrid `search_code`, `prefetch_resident_overlay` и query-triggered dirty refresh: при отказе сохранять dirty-обязательство и обслуживать последний read-only snapshot. Проверка: MCP-тест `search_code_does_not_write_after_supersession` покрывает все три ветки, takeover между prefetch/plan/apply и отсутствие чтения lease-файла либо Store-записи на чистом read-only пути.
- [x] 2.8 Сохранить публикационный барьер уже выполняющейся обычной или fused-сборки: после замеченного вытеснения временный файл текущей отклонённой публикации удаляется на каждом раннем выходе, а `bsl-graph.db` не заменяется; не добавлять широкую уборку других `*.building.*`. Проверка: `cargo test -p mcp-server graph::build::tests::superseded_build_is_discarded_after_owner_release` покрывает обычный publish refusal и fused failure до `publish_or_discard`.
- [x] 2.9 Инвертировать восстановительный контракт `OverlayRetry`: замеченное вытеснение навсегда запрещает новый проход и fenced-публикацию, disarm-ит retry и не переводит semantic runtime в `Ready`, а временный `UNCLAIMED` остаётся повторяемым; заменить `a_lost_lease_pauses_and_a_returned_lease_resumes` и обновить комментарии о возобновлении. Проверка: `cargo test -p mcp-server state::overlay_retry::tests::observed_supersession_never_resumes` и тест временной потери блокировки проверяют terminal runtime-status.
- [x] 2.10 Сохранить public `embed_pending_chunks_standalone -> Result<VectorIndex, SearchError>` как прямой wrapper и добавить используемый MCP `embed_pending_chunks_fenced -> Result<Option<VectorIndex>, SearchError>` с одним callback, который ровно один раз выполняет переданную apply-операцию и сообщает допуск. Fenced-вариант открывает обязательную существующую БД через `Store::open_existing`, коммитит каждый пакет embeddings одной SQLite-транзакцией и отдельно ограждает `persist_built`; `None` означает только requested stop или отказ callback, SQLite-ошибки остаются `Err`, а sidecar I/O сохраняет best-effort log-and-continue. Проверка: `cargo test -p bsl-search engine::tests::embedding_publish_fence` покрывает wrapper, отказ callback перед текущим batch/sidecar, отсутствие частичного batch и сохранение ранее ограждённых пакетов.
- [x] 2.11 В MCP-хосте передать во fenced embedding callback `with_ownership`, а перед `set_vector_index` брать `engine mutex` и выполнять свежую проверку со вторым `with_ownership`, охватывающим swap; при `None` не публиковать resident-индекс/sidecar или успешный runtime-status, затем при `is_superseded() == false` сохранить rerun, а при `true` прекратить проход навсегда. Проверка: `cargo test -p mcp-server state::embed::tests::embedding_fence_distinguishes_retry_from_supersession` отдельно покрывает временный lock refusal и takeover перед записью пакета, `persist_built` и swap.

## 3. Собственный снимок и MCP-контракт графа

- [x] 3.1 Обычное открытие общего пути при промахе пула выполнять внутри `with_ownership`, удерживая lease lock от свежего чтения записи до получения SQLite-дескриптора; после вытеснения разрешать `snapshot()` только через доступный в пуле собственный дескриптор и не открывать путь заново. Уже начатый запрос может завершиться на занятом дескрипторе, а после его возврата в пул чтение может возобновиться. Проверка: новый `graph::snapshot::tests::superseded_graph_serves_only_preopened_snapshot` покрывает takeover между проверкой и open, живого чужого владельца при промахе пула, временный lock refusal без терминального признака с успешным последующим повтором, временно занятый дескриптор и отказ от same-topology замены; существующий unmanaged/non-superseded тест и незамеченная краткая замена сохраняются как контроли.
- [x] 3.2 Реализовать таблицу `graph(status)`, сначала обновляя владение через `owns_caches()`, затем читая `is_superseded()`: `ready + superseded` только с доступным в пуле собственным дескриптором, иначе `failed + superseded` с общей причиной для `Idle/Loading/Failed/Ready`. Проверка: `cargo test -p mcp-server graph::state::tests::superseded_status_truth_table` также доказывает временный `owns_caches: false` без терминального статуса, отсутствие `loading` и фоновых запусков, а также возврат из `failed` в `ready` только при возврате того же дескриптора в пул.
- [x] 3.3 Возвращать ту же ошибку переподключения из всех содержательных действий без собственного снимка, сохранив доступность `schema`; проверка: `cargo test -p mcp-server graph_actions_fail_when_superseded_snapshot_is_gone` покрывает как минимум `overview`, `node`, повторный вызов и статическую схему.
- [x] 3.4 Обновить встроенное описание схемы `graph` и `docs/mcp/TOOLS_AND_EXTENSION.md`/`docs/mcp/SETUP.md`: условное чтение снимка, обе терминальные формы, одноразовый перезапуск старых бинарников и влияние краткого stdio/fallback-процесса; проверка: `cargo test -p mcp-server tools::graph::tests::schema_advertises_the_current_contract_shape` сохраняет версию `29` и существующий набор полей.

## 4. Брокер и восстановление новым подключением

- [x] 4.1 Перевести `SharedState::superseded` на последовательность «обновить владение через `owns_caches()` → прочитать `is_superseded()`», оставив разрешение записи и resident-status `owns_caches` на фактическом `owns_caches*`; проверка: `cargo test -p mcp-server state::tests::terminal_supersession_is_not_transient_nonownership` покрывает временный `owns_caches: false` без вытеснения и необратимый terminal flag.
- [x] 4.2 Перевести раннее завершение брокера на терминальный признак; проверка: `cargo test -p mcp-server --test broker superseded_backend_lifecycle` доказывает, что активный сеанс не разрывается, после последнего отключения демон завершается даже после освобождения чужой аренды, а временный `UNCLAIMED` не завершается досрочно.
- [x] 4.3 Подтвердить новый невытесненный процесс: он захватывает свободную аренду и принимает завершённый совместимый `bsl-graph.db` через штатный `try_publish_cached` без запуска построителя; проверка: `cargo test -p mcp-server graph::build::tests::fresh_generation_reuses_completed_cache`.
- [x] 4.4 Добавить Unix-интеграционный тест `crates/mcp-server/tests/superseded_daemon_lifecycle.rs` с двумя арендами и графами над одним временным кэшем: старый замечает владельца, новый атомарно заменяет граф и завершается, старый не меняет канонический граф или общее search-хранилище и отвечает `ready|failed + superseded`, третий процесс повторно использует кэш; проверка: `cargo test -p mcp-server --test superseded_daemon_lifecycle` под `#[cfg(unix)]` сравнивает идентичность/mtime графа, search DB и старого inode.

## 5. Итоговые шлюзы и эксплуатационная проверка

- [x] 5.1 Добавить в существующее Windows-задание `.github/workflows/ci.yml` целевые тесты аренды, fenced workspace constructors, broker, terminal graph status, запрета повторного открытия, sink cursor/apply и embedding fence без Unix-сценария замены открытого файла; использовать те же точные фильтры задач 1.1–4.2. Проверка: `actionlint .github/workflows/ci.yml`, а `windows-mcp` выполняет эти фильтры на `windows-latest`.
- [x] 5.2 Выполнить целевые проверки рядом с изменениями: `cargo test -p bsl-search engine::tests::workspace_constructor_publish_fence`, `cargo test -p bsl-search engine::tests::embedding_publish_fence`, `cargo test -p mcp-server workspace_lease`, `cargo test -p mcp-server superseded`, `cargo test -p mcp-server state::bootstrap::tests::superseded_bootstrap`, `cargo test -p mcp-server graph::build::tests::superseded_fused_writer`, `cargo test -p mcp-server search_code_does_not_write_after_supersession`, `cargo test -p mcp-server state::overlay_retry`, `cargo test -p mcp-server state::embed` и `cargo test -p mcp-server --test broker`.
- [x] 5.3 Выполнить общие проверки затронутых крейтов: `cargo fmt --all -- --check`, `cargo clippy -p bsl-search -p mcp-server --all-targets --all-features -- -D warnings`, `cargo test -p bsl-search --no-fail-fast` и `cargo test -p mcp-server --no-fail-fast`.
- [x] 5.4 Сверить каждый сценарий `specs/superseded-daemon-lifecycle/spec.md` с автоматическим тестом; командами `cargo test -p mcp-server graph_db::tests::writes_and_reads_back_nodes_and_edges` и `cargo test -p bsl-search store::tests::open_stamps_current_schema_version` подтвердить прежние graph/search SQLite versions, а diff по cache-layout и `Cargo.toml`/`Cargo.lock` — отсутствие нового runtime-формата и зависимости. Выполнить `git diff --check` и `openspec validate prevent-superseded-daemon-reclaim --strict --no-interactive`.
- [x] 5.5 На временном малом workspace повторить последовательность через штатный MCP-брокер с таймаутом 60 секунд на переход: после замеченного токена старый процесс не меняет graph/search, отвечает `ready|failed + superseded`, завершается после последнего disconnect без idle TTL, а третий процесс принимает готовый кэш без build. Гарантированно завершить дочерние процессы и удалить временный кэш; сохранить очищенные PID, статусы и идентичность файлов без секретов и пользовательских данных в `openspec/changes/prevent-superseded-daemon-reclaim/verification.md`.
- [x] 5.6 Только после зелёных автоматических проверок и успешного шага 5.5 закрыть инцидент командой `improve resolve 71921038-d210-4030-ab12-092f4fcbf38e --evidence <проверенные-команды-и-результат>`.
