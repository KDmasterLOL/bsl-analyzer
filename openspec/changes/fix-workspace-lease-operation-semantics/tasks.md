## 0. Входной шлюз

- [x] 0.1 Fetch `upstream/develop`, подтвердить, что рабочая ветка основана на актуальном upstream и patch-equivalent зависимому `prevent-superseded-daemon-reclaim` через ancestry/range-diff, затем выполнить strict validation обоих changes; реализацию не начинать на чистом `develop` без PR #54.

## 1. Явный outcome и короткая ограда

- [x] 1.1 В `workspace_lease.rs` заменить `Option` на `LeaseOutcome::{Applied,TransientRefusal,Terminal}`, фиксируемый внутри fence, и добавить checkpointed variant на stdlib `ControlFlow`: mid-callback release после rollback становится ровно `Terminal`, а `Continue(Err(E))` остаётся operation error. В `state/mod.rs` сделать `WorkspaceSearchApply<T,E>` с отдельным `OperationError(E)`; unit tests покрывают busy lock, `UNCLAIMED`, foreign token, release до допуска и внутри callback, checkpoint heartbeat/rollback, callback error, poisoned engine mutex и отсутствующий engine без post-hoc опроса флагов.
- [x] 1.2 Перевести production callsites и erased `bsl-search` fence adapters в `state/{bootstrap,sync,embed}.rs`, `graph/{build,snapshot}.rs` и fused sink на exhaustive outcomes; pre/post `rg 'with_ownership|apply_workspace_search|Applied\(Err'` и clippy подтверждают полный список, единственное преобразование callback error в `OperationError` и отсутствие прежнего `None -> Retry`.
- [x] 1.3 Переиспользовать существующие test hooks и добавить только blockable commit/verdict seam: regressions отличают busy/`UNCLAIMED`, foreign-token terminal и release; `release()` устанавливает terminal flag до lifecycle mutex и ждёт не дольше короткой порции либо следующего 64-element atomic-transaction checkpoint + rollback + `LOCK_WAIT`. Real `STALE_AFTER`/65-second sleeps не используются.

## 2. Ни одного workspace-sized callback под lease

- [x] 2.1 В `bsl-search` закрепить один внутренний `WORKSPACE_APPLY_BATCH_ROWS = 64` и существующий callback-adapter: test `N+1` для независимо видимых порций доказывает две fenced transactions; отдельный atomic-plan test доказывает одну fenced transaction, heartbeat/terminal checkpoint на границе 64 и полный rollback при release. Shared SQLite write transaction вне lease и новый trait/thread/schema/setting не добавляются.
- [x] 2.2 Разделить topology context refresh в `engine/store` и `state/embed.rs` на off-fence render и порции до 64 row/chunk mutations с прежним `seq_bound`; tests проверяют heartbeat при виртуальном превышении `STALE_AFTER`, сохранение завершённых порций, очистку mark только после последнего chunk path и terminal stop между fences.
- [x] 2.3 Сделать drift/full-rescan apply в `state/sync.rs` порционным и origin-aware: `TransientRefusal` сохраняет plan+apply cursor, `OperationError` продвигает hub cursor и оставляет один `rescan_required` slot, fresh drift не сбрасывает backoff, terminal завершает sink; regressions покрывают durable error, второй change, remove→recreate, bounded slot и независимые graph nudges.
- [x] 2.4 Перевести validated workspace-roots transition в `engine/store` и `state/embed.rs` на off-fence plan/in-memory staging и одну cooperatively-cancellable fenced SQLite transaction + map/index swap; tests доказывают прежнюю атомарность root table/cache/vector index, heartbeat/terminal checkpoint каждые 64 rows, отсутствие промежуточной публикации при transient/release и retry того же validated obligation.
- [x] 2.5 Разделить overlay Phase C в `workspace_overlay/engine` и `state/embed.rs`: новое cache/bundle готовится вне lease, publication baseline повторно проверяется, а shared fingerprint/embedding writes и map swap выполняются одной fenced transaction с heartbeat/terminal checkpoints; tests покрывают superseded plan, transient same-plan retry, release rollback, Store error provenance и отсутствие off-fence shared SQLite transaction.
- [x] 2.6 Перевести bootstrap Store open/migrations/clear/FTS в `store/engine` и `state/bootstrap.rs` на off-fence read/plan и fenced transactional groups; независимо видимые rows ограничены 64, атомарные schema/clear/FTS transactions обновляют heartbeat и проверяют terminal каждые 64 элемента, а init не публикует частично подготовленный engine. Проверить current-schema warm open, migration, Postgres clear и FTS rebuild на transient/release/OperationError.
- [x] 2.7 Перевести directory indexing и fused ingest в `engine/store`, `state/bootstrap.rs` и `graph/build.rs` на подготовку вне lease: независимые files получают отдельные fences, а один file остаётся атомарной checkpointed transaction. Regression с 65+ files доказывает несколько fences и сохранение завершённых files; отдельный файл с 65+ chunks доказывает heartbeat/terminal checkpoint и полный rollback без частичных chunks/hash.

## 3. Embedding без повторной сетевой стоимости

- [x] 3.1 В MCP-host один раз разобрать положительный `EMBEDDING_PUBLISH_RETRY_BUDGET_SECS` рядом с существующей embedding-конфигурацией (`600` default, warning+default для invalid/zero/checked-deadline overflow), передать `Duration` двум существующим workers без изменения public `bsl_search::SearchConfig`, документировать knob в `docs/mcp/SETUP.md`; env tests переиспользуют `ENV_LOCK`/`EnvVarGuard`.
- [x] 3.2 Перед каждым network batch collection и overlay paths выполнять fresh typed ownership gate; fenced workspace network batches идут последовательно, а direct/non-fenced concurrency остаётся настроенной. Mock-embedder regressions доказывают ноль сетевых вызовов при preflight refusal и сохранение direct concurrency > 1.
- [x] 3.3 На обоих fenced embedding paths удерживать ровно один paid batch до terminal исхода его SQLite embedding/cache commit; после всех batches отдельно готовить sidecar/index либо whole-plan Phase C bundle и при `TransientRefusal` повторять только этот несетевой bundle. Scripted fence tests считают один embedder call для отказавшего batch, не повторяют уже committed batches и не вызывают network при final publication retry.
- [x] 3.4 Переиспользовать `retry_delay` в `OverlayRetry` и collection worker с одним deadline от первого transient: coalesced signals не сбрасывают deadline/streak, budget exhaustion и `OperationError` дают `Failed` без самоповтора, terminal disarms, а только новый внешний signal после `Failed` создаёт полный новый budget. Deterministic time tests покрывают default/override, exhaustion, continuous kicks, Store/network errors и recovery signal.

## 4. Дешёвый snapshot и честный build retry

- [x] 4.1 В graph publish, incremental publish, clean cached adoption и stale cached adoption открыть/проверить `SNAPSHOT_POOL_CAP = 4` handles вне fence, затем коротким typed fence перепроверить ownership и ожидаемые generation/path identity и атомарно установить pool вместе с `Ready`; initial/open/identity failure не публикует `Ready`, reload сохраняет прежний snapshot и retry cause. Tests покрывают каждую publication path и race replace между open/admission.
- [x] 4.2 Сделать request-time `GraphState::snapshot()` строго pool-only и перевести `resolve_names`, `graph` и `symbol_info` на немедленный miss без flock/`GraphDb::open`/refill; regressions удерживают все четыре handles и lock, проверяют каждый handler отдельно и сохраняют superseded reconnect error после замены общего файла.
- [x] 4.3 Для единственного background caller в `state/sync.rs` добавить отдельный blocking snapshot acquisition: pool first, затем fresh typed preflight, не более одного open вне fence и короткая post-open проверка generation/path identity; XML-drift regression с занятым pool доказывает, что transient/identity/open error сохраняет `rescan_required/context` debt и referencing modules/context marks не превращаются в пустой успешный результат.
- [x] 4.4 Передавать классифицированную причину в `record_load_failure`, удалить повторный `may_build` и побочную установку `withheld_build` из `publish_or_discard`; tests проверяют возврат владения после `TransientRefusal`, terminal release/supersession и настоящую build/rename error при отрицательном позднем probe.

## Разрывы ревью

- [x] R1 Удалить второй transient deadline из `OverlayRetry`: `PublishRetryWindow` остаётся единственным владельцем budget внутри pass, outer state хранит только scheduling для incomplete/superseded; перенести общий helper рядом с `retry_delay` и удалить дублирующий exhaustion test.
- [x] R2 Добавить одному coalesced search rescan debt persistent `streak/next_allowed` на существующем `retry_delay`: свежие hub events объединяются без сброса backoff, ожидание ограничено ближайшим retry; deterministic continuous-event regression доказывает bounded retry и последующую convergence.
- [x] R3 Готовить vector sidecar/index artifact (`save`/fsync/hash/temp sidecar) вне lease и под коротким fence выполнять только проверку baseline и install/replace; transient повторяет тот же несетевой bundle без network/prepare, release не ждёт workspace-sized persist.
- [x] R4 Удерживать `inner` и `snapshot_pool` одновременно в порядке `inner → pool` при request checkout, чтобы concurrent install не позволял gen1 reader удалить весь gen2 pool; deterministic interleaving regression доказывает сохранение полного нового pool.
- [x] R5 Усилить prepared snapshot identity: нести expected freshness token и перепроверять его под final fence через уже открытый descriptor, добавить stable Windows file identity (`volume_serial_number/file_index`) и equal-size/time replacement regression.
- [x] R6 Сохранить typed prepare/adoption failure до `record_load_failure`: genuine invalid/open fallback отделён от transient identity/lease и terminal; clean/stale/incremental/full paths сохраняют старый reload snapshot и взводят `withheld_build` только для исходного transient.
- [x] R7 Удалить erase/downcast и дубли outcome в graph publication: один typed `LoadFailure { reason, message }` проходит prepare/install/adoption до `record_load_failure`, один `PublishAttemptOutcome` обслуживает incremental и cache adoption; `anyhow` остаётся только границей настоящих build errors.
- [x] R8 Удалить новый direct runtime dependency `windows-sys`, сохранив stable Windows volume/file identity через минимальный native API под `cfg(windows)`; lockfile не получает нового dependency edge, portable identity regression остаётся тем же.
- [x] R9 Классифицировать `NotFound` при обоих prepare identity reads как transient replacement, а остальные I/O (`PermissionDenied` и прочие) как typed open/operation failure; deterministic classifier regression не зависит от прав процесса.
<!-- GOAL_CURSOR -->
- [x] R10 Расширить XML background snapshot regression так, чтобы busy, identity-changed и open-error каждый end-to-end оставляли `rescan_required/context` debt; полные exact module-path filters обязаны запускать по одному тесту, не ноль.
  - Этап цикла: проверка
  - Состояние шага: один exact regression запускает настоящий change-hub sink; busy сохраняет непризнанный batch, identity/open признаются только в rescan debt, а retry сходится к context mark
  - Следующее действие: локальные gates завершены; live Windows gate ждёт отдельного разрешения на commit/push
  - Файлы шага: `crates/mcp-server/src/graph/{state,snapshot}.rs`, `crates/mcp-server/src/state/sync.rs`

## 5. Финальные шлюзы

- [x] 5.1 Выполнить адресные tests `workspace_lease`, `state::{bootstrap,sync,embed,overlay_retry}`, `graph::{snapshot,build}` и три MCP handler regressions на Linux; добавить те же точные portable filters в существующий Windows CI job и локально выполнить `actionlint .github/workflows/ci.yml`.
- [x] 5.2 Выполнить `cargo fmt --all -- --check`, `cargo clippy -p bsl-search -p mcp-server --all-targets --all-features -- -D warnings`, `cargo test -p bsl-search --no-fail-fast`, `cargo test -p mcp-server --no-fail-fast` и `git diff --check`.
- [x] 5.3 Сопоставить каждый scenario delta spec и все пять проверяемых инвариантов issue #71 с прямым test identifier; подтвердить отсутствие новых runtime dependency, thread/scheduler, SQLite/cache format и MCP wire change.
- [x] 5.4 Выполнить `openspec validate prevent-superseded-daemon-reclaim --strict --no-interactive` и `openspec validate fix-workspace-lease-operation-semantics --strict --no-interactive`, проверить отсутствие незакрытых решений и сохранить implementation tasks в согласованном состоянии.
- [ ] 5.5 Только после отдельно разрешённых commit/push проверить live Windows job с добавленными filters; до публикации этот внешний gate не подменяется локальным утверждением о выполненном CI.
